#include <bits/stdc++/h>

using namespace std;

typdef long long ll;

int main()
{
	int N; 
	cin >> N;
	vector<ll> cows(N);
	cows[i] = 0;
	ll add = 0;
	for(int i = 0; i < N; i++)
	{
		cin >> cows[i];
	}
	
	sort(cows.begin(), cows.end());
	
	for(int i = 0; i < N; i++)
	{
		add += cows[i] * (i + 1);
		
	}
	
	for(int 
	
	int Q;
	cin >> Q;
	// if it decreases, it will move up the line
	while(Q--)
	{
		
		ll i, j;
		cin >> i >> j;
		
		cows[i] = j;
		
		
		

	

	
	
	
	
	
